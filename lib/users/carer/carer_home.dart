import 'package:flutter/material.dart';

class CarerHome extends StatefulWidget {
  const CarerHome({Key? key}) : super(key: key);

  @override
  State<CarerHome> createState() => _CarerHomeState();
}

//TODO: will display a list of jobs and allow the carer to request to cancel jobs


class _CarerHomeState extends State<CarerHome> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
      ),
      body: Column(
        children: [

        ],
      ),
    );
  }
}
